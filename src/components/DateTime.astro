---
const dateFormatter = new Intl.DateTimeFormat('en-US', { dateStyle: 'medium' });

const { format } = dateFormatter;

const { value, size } = Astro.props;
---

<small class={`text-slate-300 text-${size ?? 'small'} font-thin`}>{format(new Date(value))}</small>
