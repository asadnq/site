---
const dateFormatter = new Intl.DateTimeFormat('en-US', { dateStyle: 'medium' });

const { format } = dateFormatter;

const { value } = Astro.props;
---

<small class="text-slate-300 text-small font-thin">{format(new Date(value))}</small>
